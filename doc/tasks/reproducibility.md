# reproducibility 模块任务

## 模块目标

整理复现实验所需的说明、依赖、命令、输出约定和报告素材，确保课程作业可以被本地重新运行和检查。

## 最小任务清单

- [x] R001 创建 `requirements.txt`，列出 pandas、numpy、scikit-learn、torch、matplotlib、pyyaml、joblib、lightgbm 可选依赖等。
- [x] R002 创建 `README.md`，说明项目目标、数据路径、环境安装、训练、评估、回测、每日预测命令。
- [x] R003 在 README 中明确数据不提交，默认数据目录为 `A股数据/`。
- [x] R004 在 README 中说明训练期、验证期、主标签和无未来信息泄露原则。
- [x] R005 在 README 中说明小股票池优先使用沪深 300，官方股票池排除 ST 和北交所股票。
- [x] R006 整理示例命令：生成特征、训练 MLP、训练 Transformer、评估预测、运行回测、生成每日信号和订单。
- [x] R007 整理输出文件说明，包括 runs、evaluation、backtest、signals、orders。
- [x] R008 创建实验记录模板，记录 run_id、模型、特征、时间区间、指标、回测结果和备注。
- [x] R009 创建报告素材导出任务，收集损失曲线、IC 曲线、分组收益图、净值曲线和核心指标表。
- [x] R010 明确不提交内容：原始数据、大体积缓存、训练权重、临时输出。
- [x] R011 增加最小复现检查清单：安装依赖、读取数据、训练 1 epoch、评估、回测、每日预测。
- [x] R012 在最终报告前检查 `doc/proposal.md`、`doc/detailed-design.md`、任务清单和 README 口径一致。

## 完成标准

- [x] 新环境按 README 可以跑通最小流程。
- [x] 依赖文件足够安装核心代码运行环境。
- [x] 输出产物和不提交内容边界清晰。
- [x] 报告所需图表和指标有明确生成路径。


